document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('agentForm');const note=document.getElementById('formNote');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const phone=data.get('phone')||'';const area=data.get('area')||'';const type=data.get('type')||'';const message=data.get('message')||'';const text=`您好 Sweet Potato 团队,%0A%0A我想申请成为代理 / 推荐合作伙伴。%0A%0A姓名: ${encodeURIComponent(name)}%0A电话: ${encodeURIComponent(phone)}%0A地区 / 客户网络: ${encodeURIComponent(area)}%0A想协助的对象: ${encodeURIComponent(type)}%0A详细说明: ${encodeURIComponent(message)}%0A%0A备注: 我明白奖励将独立处理，并且必须经过核实。`;note.textContent='正在打开 WhatsApp…';window.open(`https://wa.me/60177740471?text=${text}`,'_blank');});});