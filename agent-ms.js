document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('agentForm');const note=document.getElementById('formNote');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const phone=data.get('phone')||'';const area=data.get('area')||'';const type=data.get('type')||'';const message=data.get('message')||'';const text=`Hello pasukan Sweet Potato,%0A%0ASaya ingin memohon untuk menjadi ejen / rakan referral.%0A%0ANama: ${encodeURIComponent(name)}%0ATelefon: ${encodeURIComponent(phone)}%0AKawasan / Rangkaian: ${encodeURIComponent(area)}%0AIngin bantu dengan: ${encodeURIComponent(type)}%0AMaklumat lanjut: ${encodeURIComponent(message)}%0A%0ANota: Saya faham ganjaran diuruskan secara berasingan dan tertakluk kepada pengesahan.`;note.textContent='Membuka WhatsApp…';window.open(`https://wa.me/60177740471?text=${text}`,'_blank');});});