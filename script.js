document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('contactForm');const note=document.getElementById('formNote');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const phone=data.get('phone')||'';const interest=data.get('interest')||'';const message=data.get('message')||'';const text=`Hello Sweet Potato Team,%0AName: ${encodeURIComponent(name)}%0APhone: ${encodeURIComponent(phone)}%0AInterest: ${encodeURIComponent(interest)}%0AMessage: ${encodeURIComponent(message)}`;note.textContent='Opening WhatsApp…';window.open(`https://wa.me/60177740471?text=${text}`,'_blank');});}const copyBtn=document.querySelector('.copy-link-btn');const copyNote=document.querySelector('[data-copy-note]');if(copyBtn){copyBtn.addEventListener('click',async()=>{const link=copyBtn.getAttribute('data-copy-link')||'';try{await navigator.clipboard.writeText(link);if(copyNote)copyNote.textContent='Link copied.';}catch{if(copyNote)copyNote.textContent='Copy failed. Please copy manually.';}});}});