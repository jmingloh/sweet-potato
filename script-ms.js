document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('contactForm');const note=document.getElementById('formNote');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const phone=data.get('phone')||'';const interest=data.get('interest')||'';const message=data.get('message')||'';const text=`Hello pasukan Sweet Potato,%0ANama: ${encodeURIComponent(name)}%0ATelefon: ${encodeURIComponent(phone)}%0AMinat: ${encodeURIComponent(interest)}%0AMesej: ${encodeURIComponent(message)}`;note.textContent='Membuka WhatsApp…';window.open(`https://wa.me/60177740471?text=${text}`,'_blank');});});