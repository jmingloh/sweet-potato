document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('agentForm');const note=document.getElementById('formNote');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const phone=data.get('phone')||'';const area=data.get('area')||'';const type=data.get('type')||'';const message=data.get('message')||'';const text=`Hello Sweet Potato Team,%0A%0AI want to apply to become an agent / referral partner.%0A%0AName: ${encodeURIComponent(name)}%0APhone: ${encodeURIComponent(phone)}%0AArea / Network: ${encodeURIComponent(area)}%0AInterested In: ${encodeURIComponent(type)}%0ADetails: ${encodeURIComponent(message)}%0A%0ANote: I understand rewards are handled independently and subject to verification.`;note.textContent='Opening WhatsApp…';window.open(`https://wa.me/60177740471?text=${text}`,'_blank');});});