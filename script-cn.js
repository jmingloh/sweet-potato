document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('contactForm');const note=document.getElementById('formNote');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const phone=data.get('phone')||'';const interest=data.get('interest')||'';const message=data.get('message')||'';const text=`您好 Sweet Potato 团队,%0A姓名: ${encodeURIComponent(name)}%0A电话: ${encodeURIComponent(phone)}%0A咨询项目: ${encodeURIComponent(interest)}%0A留言: ${encodeURIComponent(message)}`;note.textContent='正在打开 WhatsApp…';window.open(`https://wa.me/60177740471?text=${text}`,'_blank');});}const copyBtn=document.querySelector('.copy-link-btn');const copyNote=document.querySelector('[data-copy-note]');if(copyBtn){copyBtn.addEventListener('click',async()=>{const link=copyBtn.getAttribute('data-copy-link')||'';try{await navigator.clipboard.writeText(link);if(copyNote)copyNote.textContent='链接已复制。';}catch{if(copyNote)copyNote.textContent='复制失败，请手动复制。';}});}});